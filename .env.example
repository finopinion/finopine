# Copy to .env for local runs. Never commit .env.
# Used only by scripts/generate-article.mjs — the site itself needs no keys.
GEMINI_API_KEY=
# Optional: contact form. Missing key makes the form degrade to an email panel.
PUBLIC_WEB3FORMS_KEY=
